# AI对话助手前端项目

这是一个基于Vue 3 + Vite的现代化AI对话前端应用。

## 项目特性

- 🚀 **Vue 3 + Vite** - 快速的开发体验和编译速度
- 🎨 **Ant Design Vue** - 专业的UI组件库
- 🛣️ **Vue Router** - 强大的路由管理
- 📦 **Pinia** - 简洁的状态管理
- 🔗 **Axios** - 优雅的HTTP请求
- 💬 **实时对话** - 与AI进行流畅的对话交互

## 项目结构

```
src/
├── api/                 # API服务模块
│   ├── request.js      # Axios实例配置
│   └── chatService.js  # 聊天API服务
├── components/          # Vue组件
│   └── ChatMessage.vue # 消息组件
├── pages/              # 页面组件
│   └── ChatView.vue    # 聊天页面
├── stores/             # Pinia状态管理
│   └── chatStore.js    # 聊天状态
├── utils/              # 工具函数
├── router/             # 路由配置
│   └── index.js        # 路由定义
├── App.vue             # 主应用组件
├── main.js             # 应用入口
└── style.css           # 全局样式
```

## 快速开始

### 前置要求

- Node.js >= 14
- npm 或 yarn

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

启动后打开浏览器访问 `http://localhost:5173`

### 生产构建

```bash
npm run build
```

构建生成的文件位于 `dist/` 目录

### 预览生产版本

```bash
npm run preview
```

## 配置

### 环境变量

项目根目录下的环境变量配置文件：

- `.env` - 通用配置
- `.env.development` - 开发环境配置
- `.env.production` - 生产环境配置

**VITE_API_BASE_URL** - API基础URL

```env
# 开发环境
VITE_API_BASE_URL=http://localhost:8000/api

# 生产环境
VITE_API_BASE_URL=/api
```

### API端点

聊天API期望的后端接口：

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/chat/send` | 发送聊天消息 |
| GET | `/chat/history` | 获取聊天历史 |
| POST | `/chat/clear` | 清空聊天记录 |

**请求示例：**

```javascript
// 发送消息
POST /chat/send
{
  "message": "你好"
}

// 响应
{
  "message": "你好！有什么我可以帮助你的吗？"
}
```

## 核心组件

### ChatMessage 组件

显示单条聊天消息，支持用户消息和AI回复的不同样式。

**Props:**
- `message` (Object) - 消息对象，包含 `role`, `content`, `timestamp` 属性

### ChatView 页面

主聊天界面，包含：
- 消息列表显示
- 消息输入框
- 发送按钮
- 清空对话功能
- 自动滚动到最新消息

## 状态管理

使用Pinia管理聊天状态：

```javascript
const chatStore = useChatStore()

// 获取消息列表
chatStore.messages

// 添加消息
chatStore.addMessage({ role: 'user', content: 'Hello' })

// 设置加载状态
chatStore.setLoading(true)

// 清空消息
chatStore.clearMessages()
```

## 样式系统

项目使用：
- **Ant Design Vue** - 组件样式
- **CSS Modules** - 组件作用域样式
- **CSS Variables** - 全局主题变量

## 常见问题

### Q: 如何修改API端点？
A: 修改 `.env` 或 `.env.development` 文件中的 `VITE_API_BASE_URL` 变量。

### Q: 如何添加新页面？
A: 
1. 在 `src/pages/` 中创建新的Vue文件
2. 在 `src/router/index.js` 中添加路由配置

### Q: 如何添加新API服务？
A: 在 `src/api/` 中创建新的服务文件，参考 `chatService.js` 的结构。

## 开发建议

1. 遵循Vue 3 Composition API的最佳实践
2. 使用 `<script setup>` 语法简化组件
3. 在stores中集中管理应用状态
4. 在API服务中处理错误和数据转换
5. 使用Ant Design Vue的主题定制功能统一UI

## 许可证

MIT

## 联系方式

项目问题或建议，请提交Issue。
